# Web-Scraper-for-Headlines
A Python-based news scraper that collects headlines from multiple websites using requests and BeautifulSoup. It extracts titles, URLs, and timestamps, supports keyword filtering, respects robots.txt, uses delay and retry logic, and saves results in JSON or CSV for easy analysis.
